[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npx --yes pnpm@10 install --frozen-lockfile"
]

[phases.build]
cmds = [
  "npx --yes pnpm@10 run build"
]

[start]
cmd = "node dist/index.js"
